# Review Report

## Stage 1 自动审查

### PRD 对齐

- [x] 实现卷资料审核后继续生成整卷组纲计划、组纲和组纲审核。
- [x] 实现组纲审核通过后才标记当前卷资料适配完成。
- [x] 实现章节工作流按已审核动态组纲计划分组。
- [x] 移除组生成、组审、卷级审核中的参考源章节正文输入。

### 代码质量

- [x] 组纲计划路径和 manifest 逻辑集中在 `novelist.core.group_outline_plan`。
- [x] `novel_adaptation` 新增阶段集中在 `adaptation/group_outlines.py`，runner 只负责编排。
- [x] `novel_chapter_rewrite` 保留兼容函数名，但运行分组来源已切换为组纲计划。
- [x] 组纲在章节阶段只读冻结，避免跨阶段职责混淆。

### 安全与稳定性

- [x] 未新增密钥、token 或外部凭据。
- [x] 缺少组纲计划、计划未审核、组纲文件不完整时均阻断。
- [x] 组纲审核失败不会提前进入章节生成。
- [x] 旧工程不会静默回退到固定 5 章。

### 测试质量

- [x] 覆盖卷资料审核后 processed 延迟标记。
- [x] 覆盖动态组纲计划写入与审核。
- [x] 覆盖章节工作流动态分组和缺计划阻断。
- [x] 覆盖组生成/组审/卷审无参考源章节正文输入。

### 审查结论

- 结论：通过
- 修复项：无剩余阻塞。遗留 `five_chapter` 命名为兼容 API，可在后续单独做命名迁移。
